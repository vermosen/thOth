# pragma once

#include <cstddef>

namespace thOth {

	typedef std::size_t size;				// size

	typedef double real;					// real number

	typedef long volume;

	enum marketSide : bool {

		bid = true,
		ask = false

	};

}
